[workspace]
members = ["cyclonedds-sys", "cyclonedds", "cyclonedds-derive", "cyclonedds-test-suite", "cyclonedds-build", "cyclonedds-idlc", "cyclonedds-cli"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Seu Nome"]
license = "MIT/Apache-2.0"

[workspace.dependencies]
cyclonedds-sys = { path = "cyclonedds-sys" }
cyclonedds = { path = "cyclonedds" }
cyclonedds-derive = { path = "cyclonedds-derive" }
cyclonedds-build = { path = "cyclonedds-build" }
